export interface DistrictData {
  id: string
  name: string
  lat: number
  lon: number
}

export const processDistrictData = (data: any): DistrictData[] => {
  // JSON 데이터 처리 로직
  return []
}
